This python code generates payload (with random values) from a generic JSON schema.
A sample schema (Test Schema.json) has been added to the directory for testing purposes.
The code can be easily modified/customized for any additional scenarios.
